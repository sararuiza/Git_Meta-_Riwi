# comandos para hacer una conexion remota

- git remote add nombre_conexion url_conexion

# comando para listar las conexiones remotas
- git remote -v

# comandos renoembrar la rama
- git branch -M nombre